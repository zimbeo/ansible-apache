---
# These tasks install http and the php modules.

- name: Install http and php etc
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  with_items:
   - httpd

- name: http service state
  service:
    name: httpd
    state: started
    enabled: yes